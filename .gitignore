# Env / secrets
.env
.env.local
.env.*.local
*.env
!.env.example

# Go
gateway/bin/
*.test
*.out
coverage.*
*.prof

# Node
node_modules/
.next/
dist/
build/

# OS / IDE
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json

# Logs / temp
*.log
tmp/
.cache/

# Stray bare-name binaries from `go build` without -o (binary name == parent dir name)
workers/stubs/go/go
workers/stubs/*/main

# Private internal docs that shouldn't be public (product strategy / handoffs / plans)
docs-internal/handoff-*.md
docs-internal/plans/

# Agent harness state (auto-generated by Claude Code / ECC tooling, not project source)
AGENTS.md
.agents/
.bg-shell/
.gsd/
.opencode/
.claude/
skills-lock.json
tsconfig.tsbuildinfo
