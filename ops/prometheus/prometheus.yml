global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: crucible-gateway
    static_configs:
      - targets:
          - gateway:9090   # METRICS_PORT inside the docker network
